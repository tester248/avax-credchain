{
  "name": "credchain-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "infra:start-local": "ts-node scripts/deploy-subnets.ts",
    "infra:deploy-mock-teleporter": "ts-node scripts/deploy-teleporter.ts",
    "build": "tsc -p .",
    "lint": "echo \"No linter configured\""
  },
  "dependencies": {
    "@avalanche-sdk/client": "^0.0.4-alpha.14",
    "@avalanche-sdk/interchain": "^0.1.1-alpha.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0",
    "solc": "^0.8.21"
  }
}
