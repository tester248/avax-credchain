{
  "name": "credenchain-onchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p ./",
    "test": "hardhat test",
    "compile": "hardhat compile",
    "typechain": "hardhat run --no-compile scripts/typechain.ts",
  "deploy": "ts-node --transpile-only scripts/deploy.ts",
  "deploy:multi": "ts-node --transpile-only scripts/deploy-multi.ts"
  ,
  "export:artifacts": "ts-node --transpile-only scripts/export-artifacts.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.0.0",
    "chai": "^4.3.7",
    "hardhat": "^2.18.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "dotenv": "^16.0.0",
    "@openzeppelin/contracts": "^4.9.3"
  }
}
